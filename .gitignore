# Rust build output
/target

# Secrets / local config — never commit
.env
.env.*
!.env.example

# Local runtime state (event-log DB, snapshots)
.medha/

# personal research/notes at repo root — never commit
learn-self-evolving-agents.html
SELF_EVOLVING_AGENTS_RESEARCH.md

# per-user harness config; medha.lock.example is the shared template
medha.lock

# macOS
.DS_Store

# local, per-developer working ledgers (audit + post-release gaps) — kept out of the repo
/audit/
