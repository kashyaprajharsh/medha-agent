# Assets

`demo.gif` — the TUI recording embedded at the top of the project README.

Record it with:

```sh
asciinema rec demo.cast
agg demo.cast demo.gif --font-size 16 --theme asciinema
```

Keep it under ~5 MB so GitHub renders it inline, and roughly 820px wide to
match the README embed.
